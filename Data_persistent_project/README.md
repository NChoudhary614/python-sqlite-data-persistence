# Data Persistence Project

## Description
A simple Python project demonstrating data persistence using SQLite, with backup and restore functionality.

## Usage
- Run `main.py` to initialize the database, add sample data, and create a backup.
- Backups are stored in the `backups/` directory.
- To restore, use the `restore_database()` function in `main.py` with the path to your backup file.
